[project]
name = "note-cli"
version = "0.1.0"
description = "Tiny notes CLI used as an auto-pilot eval fixture."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
