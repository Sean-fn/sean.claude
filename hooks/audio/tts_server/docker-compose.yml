services:
  tts-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tts-server
    restart: always
    ports:
      - "7865:7865"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
